# 积分会员 - 表清单

**业务域**: points_membership
**表数量**: 10 张
**最后更新**: 2026-01-30

---

## 📊 表分层统计

- **DIM**: 1 张表
- **DWD**: 3 张表
- **DWS**: 3 张表
- **RPT**: 3 张表

---

## 🎯 表选择决策树

### 如何选择正确的表？

```
你的需求是什么？
│
├─ 需要查看某个用户的积分余额
│  └─ ✅ 使用 RPT 层: rpt_faive_gensmo_membership_points_metric_inc_1d
│     (最快，已按用户聚合)
│
├─ 需要分析积分消耗/赚取的明细（如按功能类型、按时间段）
│  ├─ 需要 consume_type 等明细维度？
│  │  └─ ✅ 使用 DWD 层: dwd_favie_gensmo_membership_consume_point_inc_1d
│  │     (包含所有明细字段，如 consume_type, device_id)
│  │
│  └─ 只需要按日期的聚合趋势？
│     └─ ✅ 使用 DWS 层: dws_faive_gensmo_membership_consume_points_metric_inc_1d
│        (已按日期聚合，性能更好)
│
└─ 需要制作报表或看大盘指标
   └─ ✅ 使用 RPT 层相关表
      (最优性能，已完成所有聚合)
```

### 核心原则

1. **优先使用高层表**：RPT > DWS > DWD
   - RPT 层性能最优，数据量最小
   - 只有需要明细字段时才使用 DWD

2. **DWD 层使用场景**：
   - ✅ 需要 consume_type / earn_type 等分类维度
   - ✅ 需要 device_id / platform 等设备信息
   - ✅ 需要查看单条记录的详细信息
   - ⚠️ 注意：必须限制时间范围（如最近 7-30 天）

3. **DWS 层使用场景**：
   - ✅ 按日期趋势分析
   - ✅ 不需要细分维度，只需总量
   - ✅ 时间范围较长（如 90 天）但不需要明细

4. **RPT 层使用场景**：
   - ✅ 查询单个或少量用户
   - ✅ 制作日报、周报
   - ✅ 大盘监控指标

### 示例对照

| 需求 | 推荐表 | 层级 | 原因 |
|------|--------|------|------|
| 最近7天各功能消耗积分排行 | dwd_consume_point | DWD | 需要 consume_type 维度 |
| 每日积分消耗趋势（30天） | dws_consume_points_metric | DWS | 只需日期聚合，不需细分 |
| 查询用户A的当前积分余额 | rpt_points_metric | RPT | 单用户查询，性能最优 |
| 积分消耗失败的订单明细 | dwd_consume_point | DWD | 需要 consume_status 过滤 |

---

## 维度层 (DIM)

| 表名 | 说明 |
|------|------|
| `dim_gem_membership_view` | --- |

---

## 明细层 (DWD)

| 表名 | 说明 |
|------|------|
| `dwd_favie_gensmo_membership_consume_point_inc_1d` | --- |
| `dwd_favie_gensmo_membership_earn_point_inc_1d` | --- |
| `dwd_favie_gensmo_membership_process_node_inc_1d` | --- |

---

## 汇总层 (DWS)

| 表名 | 说明 |
|------|------|
| `dws_faive_gensmo_membership_consume_points_metric_inc_1d` | --- |
| `dws_faive_gensmo_membership_earn_points_metric_inc_1d` | --- |
| `dws_faive_gensmo_membership_points_metric_inc_1d` | --- |

---

## 报表层 (RPT)

| 表名 | 说明 |
|------|------|
| `rpt_faive_gensmo_membership_consume_points_metric_inc_1d` | --- |
| `rpt_faive_gensmo_membership_earn_points_metric_inc_1d` | --- |
| `rpt_faive_gensmo_membership_points_metric_inc_1d` | --- |

---


**生成时间**: 2026-01-30 15:33:23
**自动生成**: 本文档由脚本自动生成
