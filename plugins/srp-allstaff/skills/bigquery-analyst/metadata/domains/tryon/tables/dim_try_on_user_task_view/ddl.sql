CREATE VIEW `srpproduct-dc37e.favie_dw.dim_try_on_user_task_view`
AS SELECT  
        t._id,
        t.created_time,
        t.last_updated,
        t.publish_time,
        t.detail_title,
        t.detail_tag,
        t.detail_description,
        t.garment_type,
        t.model_id,
        t.model_type,
        t.gender,
        t.image_url,
        t.mid_images,
        t.try_on_cover_image,
        t.try_on_mid_image,
        t.try_on_original_url,
        t.try_on_task_id,
        t.try_on_url,
        t.avatar_url,
        t.user_id,
        t.created_user_id,
        t.user_image_tag,
        t.user_image_url,
        t.is_vibe,
        t.is_feed,
        t.use_default_model,
        t.is_clone_task,
        t.source,
        t.liked_count,
        t.remix,
        t.saved_count,
        t.shared_count,
        t.score,
        t.products,
        t.outfit_description,
        t.moodboards,
        t.query,
        t.reasoning,
        t.status,
        t.type,
        t.video_try_on_mode,
        t.videos_url,
        t.f_source,
        t.f_version,
        t.from_feed,
        t.publisher,
        t.tryon_router,
        t.moderation_status,
        t.cover_image_list
    FROM `favie_dw.dim_try_on_task_view` t
    LEFT JOIN `favie_dw.dim_gem_bot_account_view` b
        ON coalesce(t.created_user_id, t.user_id) = coalesce(b.id, '')
    WHERE b.id IS NULL  -- 筛选出不在机器人账户中的任务
      AND t.created_user_id IS NOT NULL;