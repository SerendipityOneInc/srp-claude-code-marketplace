CREATE TABLE `srpproduct-dc37e.favie_dw.dwd_favie_gem_workflow_inc_1d`
(
  trace_id STRING,
  traceparent STRING,
  task_id STRING,
  raw_query STRING,
  query_modality STRING,
  rewrite_queries ARRAY<STRING>,
  image_url STRING,
  image_description STRING,
  image_height INT64,
  image_width INT64,
  user_image_tag STRING,
  collage_title STRING,
  intention STRING,
  reasoning STRING,
  query_source STRING,
  device_id STRING,
  user_type STRING,
  is_internal_user BOOL,
  user_login_type STRING,
  user_tenure_type STRING,
  country_name STRING,
  platform STRING,
  app_version STRING,
  appsflyer_id STRING,
  ad_source STRING,
  ad_campaign_id STRING,
  ad_group_id STRING,
  ad_id STRING,
  log_timestamp TIMESTAMP,
  receive_timestamp TIMESTAMP,
  dt DATE
)
PARTITION BY dt;